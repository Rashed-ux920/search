{
  "name": "search",
  "version": "1.0.0",
  "description": "this is the example to search in json file",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch index.json"
  },
  "author": "rashed",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.1"
  }
}
